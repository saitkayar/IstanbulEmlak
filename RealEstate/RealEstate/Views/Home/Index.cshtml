
@{
    ViewData["Title"] = "Home Page";
    Layout = "NewFolder/_Layoutt";
}

@*<div class="text-center">
 <a asp-controller="Auth" asp-action="Login">Admin</a>
</div>
*@


@*<table class="table">
    <thead>
        <tr>

            <th scope="col">#</th>
            <th scope="col">İlçeler</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <th scope="row">@item.Id</th>
                <td>@item.LocationName</td>

            </tr>
        }
    </tbody>
</table>*@

    <div class="row mb-5 ">
        <div class="col-3">
            <ul class="list-group">
                @foreach(District loc in @ViewBag.ds){  <li class="list-group-item">@loc.LocationName</li>}

 
</ul>
        </div>
        <div class="col-9">
        <div id="portfolio-grid" class="row no-gutter" data-aos="fade-up" data-aos-delay="200">
            @foreach (HouseViewModel item in @ViewBag.hw)
            {
                  <div class="item web col-sm-6 col-md-6 col-lg-6 mb-4">
            <a href="/Home/Detail" class="item-wrap fancybox">
              <div class="work-info">
                <h3>Detaya Git</h3>
               
              </div>
              <img class="img-fluid" src="/Resimler/@item.Image" style="width:419px;height:419px;">
            </a>
          </div>
            }
            </div>
        
        </div>
        </div>